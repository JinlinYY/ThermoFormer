# Formal checkpoints

Best ThermoFormer checkpoints are written as `checkpoints/<protocol>/seed_<n>/best_model.pt`. Binary model files are intentionally ignored by Git; their manifest records the exact Git commit, split file, dataset digest and configuration required to reproduce them.
