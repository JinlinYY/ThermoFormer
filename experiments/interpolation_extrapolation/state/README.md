# State interpolation and extrapolation

Each protocol partitions states within an unordered chemical system. Composition interpolation holds out interior states bracketed by training states; edge/T/P extrapolation uses strict non-overlapping tails and records skipped systems that lack enough unique values.
