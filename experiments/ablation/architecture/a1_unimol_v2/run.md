# A1a Uni-Mol v2 only

This immutable comparison reuses the original five-seed Uni-Mol v2 formal runs on the identical committed splits and training protocol; it is not retrained after selecting the hybrid representation.

Config: `experiments/ablation/architecture/a1_unimol_v2/config.json`.

The ablation suite recognizes this variant as a frozen reference and does not overwrite its artifacts.
