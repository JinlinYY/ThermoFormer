# Formal architecture ablations

A0 is the immutable Full reference. A1--A6 each alter one controlled modeling
choice; A7 is omitted because no additional scientifically distinct solver removal
exists beyond A6 direct VLE prediction. All runnable commands are recorded in each
variant's `run.md` and share `scripts/run_ablation_suite.py`.
