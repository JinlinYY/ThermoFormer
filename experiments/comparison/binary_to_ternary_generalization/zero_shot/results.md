# Results

Status: not run. Model selection uses binary validation only. The held-out ternary test set is exactly the same as the positive-data scaling experiments for that seed, and contains no ternary labels used for training or validation.
