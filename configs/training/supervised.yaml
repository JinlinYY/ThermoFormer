{
  "extends": "../model/c1_three_view_vanilla.yaml",
  "name": "c1_supervised",
  "training": {
    "epochs_supervised": 80,
    "epochs_physics": 0,
    "minimum_physics_epochs": 0
  },
  "runtime": {
    "output_dir": "runs/public/c1_supervised",
    "results_file": "runs/public/c1_supervised/results.md"
  }
}
