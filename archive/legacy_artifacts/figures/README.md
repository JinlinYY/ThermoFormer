# Generated paper figures

Publication figures are generated from committed result CSV/JSON files. Each figure is saved as both vector PDF and raster PNG together with the plotting source under `scripts/`.
