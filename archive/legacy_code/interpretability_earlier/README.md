# Earlier interpretability pipeline

This directory preserves the analysis, plotting, reporting, and runner modules
used for an earlier interpretability study. That study produced the superseded
four-panel artifact retained under `archive/legacy_artifacts/interpretability_earlier/`.

The manuscript Figure 2 workflow is implemented by
`src.thermoformer.interpretability.c1_final` and run through
`scripts/run_interpretability.py`.
