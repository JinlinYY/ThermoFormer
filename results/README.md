# Formal result artifacts

Each protocol writes `seed_<n>/predictions.csv`, `metrics.json`, `manifest.json` and `resolved_config.json`, then a complete five-seed run creates `metrics_by_seed.csv` and `metrics_summary.csv`. Diagnostic/smoke results do not belong here.
